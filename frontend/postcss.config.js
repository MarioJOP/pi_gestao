import tailwindcss from "tailwindcss"; // Importar o Tailwind CSS corretamente
import autoprefixer from "autoprefixer";

export default {
  plugins: [
    tailwindcss(), // Use o Tailwind CSS
    autoprefixer(), // Use o Autoprefixer
  ],
};
